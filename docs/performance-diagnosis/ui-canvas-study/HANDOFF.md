# Combined empty-native/UI canvas screen — partial evidence, corrected journal gate pending

2026-09-25. QA source07edde2c2b748fe6b0e999b8abfafd01dd10879e, run36112848069. Exact immutable DEV15.9 resources with occupancy revision in both modes. Combined candidate disables the native viewport's empty2D canvas and reparents three CompanionInterface children under HUD; original empty layer is parked on a disabled2x2 viewport. This is a reversible QA prototype, not a general production UI implementation.

Artifacts10852889556 (webkit1,27507744bytes) and10853967682 (webkit2,27505345bytes) were downloaded with exact size and ZIP integrity verified. Both reports have passed=false. Each completed96 successful checks and18 exact image pairs before a later journal predicate timeout. All12 retained PNGs independently inspected; all36 full-image pairs exact. All12 mining windows retained. Baseline/candidate weighted FPS50.066847/52.618691 (+5.10%) and59.730040/59.814820 (+0.14%, refresh ceiling). Paired p95 mixed, substantial drift. Actual synchronization calls4→2 per rendered frame (checkFramebufferStatus denominator; some RAFs contain no rendering). This is combined native+UI work reduction, not isolated UI gain or a phone forecast.

IMPORTANT CORRECTION: the failed test checked commerce_panel.is_open and commerce_panel.close_button. main._open_commerce(...,'companion') actually opens CompanionInterface.journal. Therefore the timeout does NOT demonstrate broken input. A targeted immutable-package diagnosis d41cc09fb5c356064ec3f31b8412635ff26c9881/run36113664590 also recorded commerce_open=false throughout, but actual after-0.png visibly shows the open companion journal. Its green job means the diagnostic completed, not that interaction assertions passed. That diagnosis's first original touch demonstrably opened the journal; do not infer all candidate/close cases from those incorrect fields.

Corrected source36449d641db5578eee4ea24709c0a2d6d416d0ac/run36114525488 now reads journal.is_open, actual CloseJournal and journal tabs. It runs baseline/candidate/restored real touches, all three tabs and exact journal/closed image comparisons, with NO repeated FPS matrix. Pending at this checkpoint. Broader UI lifecycle and production architecture remain unapproved.

Earlier source d01246d/run36112569683 failed restoration because Godot4.7.2 rejects a null custom_viewport argument. 07edde uses a non-Viewport Node to reset custom_viewport to null via the exact engine setter. First two images were exact but this earlier run was not acceptance evidence. Independent-review.json preserves the corrected limitations.

Corrected journal run36114525488/source36449d6 completed but failed exact journal parity after the original journal opened by real touch. Candidate difference max177/255,2637807changed channels, mean2.24938/255; no runtime errors. Original/candidate/restored image hashes were recorded but this first baseline iteration unfortunately did not retain PNGs. Do not reinterpret this as an input failure. A narrow capture-only replay25b11579bf610909a0d329ba1ee978995dbfb67f on codex/ui-touch-study-20260925 reuses exactly the36449d6 package and now retains all three actual images. It does not repeat FPS measurement or modify game code. Until inspected and resolved, UI candidate remains unapproved.

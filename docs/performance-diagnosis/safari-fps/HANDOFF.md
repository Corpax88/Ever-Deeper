# Simulator Safari recovery — real page reached, WebGL context lost

2026-09-25. Source7a236e5147fa4f12da023fb350ca7660479c622a, run36110103989, candidate10851884805, build10853200050, evidence10853271217 (2751713bytes, exact size andZIP integrity verified). Exported input/touch/build-flavor passed. This is an actual iPhoneAir Simulator/iOS26.2 Safari app via native XCTest, Xcode26.2 build17C52, not a physical phone. UA reports Safari26.2 with legacy iPhoneOS18_7 string. Landscape CSS776×356/DPR3.

The new simctl openurl route reached the actual local game document and HTTP bridge; the prior unchanged route had remained on Safari StartPage. Native Appium session and orientation succeeded. However, the game lost its WebGL context during loading; the actual failure screenshot visibly says WebGL context lost, please reload the page. No fixture menu/native-hero/mining state and zero FPS windows were recorded. This is not a successful mobile gameplay test. Bridge stopped updating~15seconds after document navigation; Appium subsequently timed out at60seconds without a native command, after the observed page/context failure. Do not claim that timeout caused the earlier context loss.

One targeted recovery is running with the exact same candidate, no rebuild: source57e766a9acd23e84964fdd8843e5f8161ac89edf/run36111409066. It installs telemetry before engine startup, records actual context attributes/renderer/limits, console errors/alerts and WebGL loss events, preserves native session for900seconds, and permits exactly one guarded page reload after context loss. Native modal alert is logged by the QA harness to keep diagnostics running. A second context loss fails early. System WebKit/GPU/jetsam logs are retained on exit. If successful, the same8mining windows run; otherwise read the concrete failure before choosing any further route. This recovery does not change game renderer attributes or reduce quality.

First uncorrected startup source1cb92f9dd336095c700fa000e9bec07e2881e2ad/run36109531664 was cancelled because the initial bridge could create a GL context before Godot; do not use its performance. Corrected7a236e avoids that. Current recovery observes getContext calls without creating an extra context. All unresolved/failure evidence is retained, no public release.

## Follow-up recovery result and final telemetry correction

Recovery57e766a9/run36111409066 did not reach menu or collect FPS. Evidence10853408103 is3609496bytes; exact ZIP size/integrity verified and actual failure PNG inspected (game loading splash, not gameplay). Two document loads occurred. Console contains partial canvas shader source output, but no complete compiler error/termination reason survived. System log extraction timed out; do not infer memory pressure, GPU crash or root cause from it.

A harness limitation was found: it recorded the first WebGL2 context (300×150, browser-default attributes), which may be the engine feature probe rather than the actual game canvas. Its lost state cannot be assigned to the game. Per-line console fetches also flooded requests and failed to retain critical events. Native screenshot completed on disk although its timed-out command did not append capture metadata. Report remains failed.

One final targeted correction is running, ee2e22509db1f840e5f23c5eb35dee7ea8a8b310/run36112922230: observe only canvas id=canvas, ignore feature-probe context-loss events, batch console/events in the existing5Hz bridge, and allow300seconds for initial load plus the single guarded reload. Same immutable candidate, same native session and visual quality. Do not repeat this route unchanged after a concrete result. Physical device claims remain unavailable.


## Final targeted route result — unavailable, no valid FPS windows

Final ee2e22509db1f840e5f23c5eb35dee7ea8a8b310/run36112922230 failed before Safari opened the game. Artifact10853983131,605999bytes, exact size and ZIP integrity verified; failure screenshot independently viewed by root shows the iOS home screen. Node fetch failed while creating the Appium/XCUITest session. Driver log stops after Simulator setup; there is no returned native-session check, no game document request, no canvas event and no FPS window. The captured system log does not establish a game GPU crash or memory failure. Do not call this an additional reproduced WebGL failure: it never reached the page.

Earlier recovery57e766/run36111409066 reached loading again but produced no gameplay. Its context flag tracked a possible feature-probe canvas, so that flag does not establish loss of the actual game canvas; per-line HTTP console flooding also lost events. Final telemetry narrowed observation to canvas#canvas, batched event transport, allowed at most one actual-canvas recovery and extended menu startup to300s, but those changes could not be evaluated because the native session never completed. Original7a236e/run36110103989 did show the actual page's context-loss alert. Across these attempts there is no mobile gameplay or FPS evidence. Stop repeating the same Simulator route without a new concrete environment remedy; continue valid Mac WebKit tests and retain this limitation.

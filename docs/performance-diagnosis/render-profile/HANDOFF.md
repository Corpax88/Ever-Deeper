# WebKit render submission profile — synchronization target found

2026-09-25. QA source525a55be87f6650d0221cf260a51093688628fbd, branch codex/render-profile-20260925, run36111016427, artifact10852918127 (8442502bytes, exact download size and ZIP integrity verified). Reused immutable fixed-light-candidate ef660efba2f17932069fa7cdd85e354fa2a549aa/run36109186370 without rebuilding. Original engine and resources retained. Occupancy revision is enabled in both light modes. Mac15/AppleGPU/WebKit26.5,2328×1260/DPR3.

91 checks passed,8 held-mining windows, exact baseline/restored visual comparisons. Both actual retained PNGs independently inspected. Four combinations (baseline/lights-off × instrumentation off/on), two symmetric windows each. All lights-off views are diagnostic, not visual candidates. Method tables and RAF arrays are empty in uninstrumented windows; instrumented windows have409/550/499/454 RAF samples and substantial actual draw calls.

WeightedFPS: baseline uninstrumented44.3751, instrumented42.2337; lights-off uninstrumented54.4627, instrumented51.0746. Instrumentation is measurable and time drift persists. Keep every window, including44.98/43.77 baseline controls and59.51/49.42 lights-off controls. No precise production cost percentage or physical-iPhone claim follows.

Every baseline RAF executes4 fenceSync and4 getSyncParameter calls. Combined native-call CPU wall time is1.726 and1.412ms per captured RAF. Lights-off still has~4 each, with combined0.896 and1.198ms/RAF. One checkFramebufferStatus per frame costs~0.44ms in baseline. Whole RAF callbacks average11.00/9.93ms baseline and8.39/9.50ms lights-off. NativeGL timing includes browser/driver synchronization waits, not GPU elapsed time; the remaining RAF time includes instrumentation accounting and must not be called pure game CPU. Engine and observer interval boundaries differ, so do not divide GL totals by engine frame counts. Timing resolution is1ms; aggregates/call counts matter more than individual short calls. AllWebGL contexts are covered, including native actor3D.

ExactGodot4.7.2 source drivers/gles3/rasterizer_canvas_gles3.cpp places one fenceSync at the end of canvas_render_items and queries buffer sync on subsequent use. RendererViewport::_draw_viewport in servers/rendering/renderer_viewport.cpp gates2D work on disable_2d. RenderingServer.viewport_set_disable_2d is a public documented API: https://docs.godotengine.org/en/stable/classes/class_renderingserver.html#class-renderingserver-method-viewport-set-disable-2d . This supports an empty-canvas investigation, not a guarantee that removing one call saves one quarter of its measured wait time.

Next bounded test: codex/empty-canvas-study-20260925,96e3519ef2f470310379125250959c908f750379,run36111698762. QA helper finds the existing NativeRig200px SubViewport only when it contains zeroCanvasItems, then reversibly disables its2D canvas pass. It retains all native source/assets/3D/animation/world lighting/UI/resolution. Candidate and restored images must be pixel-exact. Minimal GL counters verify whether4sync pairs become3. No production modification/publication or phone test yet.

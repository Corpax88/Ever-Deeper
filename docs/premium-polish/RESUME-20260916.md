# Continuation verified 16 September 2026

The September 15 Library pointer at `03b7bcf` was stale. The current remote work
branch was verified at `4e1bb94531318ebe7a68d2ccd6eddf9a6df9bae7` before changes.
The retained working tree contained the fourth milestone described in
[iteration-four-20260915/README.md](iteration-four-20260915/README.md).

All 81 changed/untracked paths from that working tree were copied into an
isolated checkout and compared by SHA-256. The original checkout was left intact.
This checkpoint preserves that milestone; it does not certify final polish.

Verification rerun on the recovered fourth milestone:

- Godot `4.7.2.stable.official.ed1daf0bf` import succeeded.
- All 15 current source cases passed, including the continuous-world journey.
- Protected invariants passed: 1,095 files and 57 QA entries.
- Actual `review_mining_contact.gd` passed under authenticated Xvfb/Mesa llvmpipe
  at 1696×780. New captures show the four station approaches, impact and final
  follow-through, full machine framing, rotation recovery and return to play.
- The room and resonance captures were visually inspected. These are native
  software-rendered captures, not physical iPhone performance evidence.

Source and contact reports are retained in `resume-20260916/`.
The outstanding priorities remain sustained FPS, full native animation
transitions and environment integration. Production v28 atlases are unchanged.
No new game version is published by this checkpoint.

## Further local work

The independent performance review found that every Deep terrain strip shared
one global revision. A hit plus its Crusher wave invalidated 326 cached draw
sections; a pet dig also invalidated 326. A bounded candidate uses ordered
per-strip signatures of local floor topology, a one-cell neighbor halo, local
damage, world seed and streaming depth. It leaves the rendering, artwork,
light/shadow treatment and command order intact.

The first candidate reduces those mutation redraw counts to 26 and 19.
All 20 candidate-versus-forced-fresh native terrain pairs are pixel-identical,
including mining, pet/Crusher state, resource depletion/respawn, stream rebasing,
and row/strip boundaries at rows 1, 21 and 64. The parity fixture deliberately
hides HUD/pickup overlays and pauses pending tweens to compare terrain rather
than unrelated animation. An independent critic inspected the source and actual
captures and confirmed the zero-difference result. This is bounded correctness
approval, conditional on the net-cost measurement, not whole-game acceptance.

The follow-up `premium-core` and `one-point-zero-world` suites pass.
Draw-callback timing is opt-in; ordinary play does not sample those timers.
The local native 60-second A/B/A workload passes functionally in all three runs:

| Deep workload | Average FPS | Worst 30-second p95, ms |
| --- | ---: | ---: |
| Existing whole-world revisions A | 38.984 | 32.775 |
| Local-strip candidate B | 38.974 | 30.685 |
| Existing revisions A2 | 39.002 | 32.156 |

The candidate is retained for reduced mutation work and exact terrain parity.
There is **no measurable average-FPS gain** in this workload, and none passes
50 FPS. The p95 difference is a single diagnostic comparison, not device
acceptance. All use the same unchanged held-mining workload, autosaves, pet,
1696×780 native viewport and Mesa llvmpipe. Source-file fingerprints, per-window
results and functional travel/mining counts are in `cache-session-comparison.json`.

The hub performance harness also had a workload defect: it chased a moving
target at full hero speed, repeatedly overshooting and reversing. Its route now
uses the target velocity plus positional correction through the real analog
input. Reports identify this as `hub_curve_feedforward_v2`; its FPS must not be
compared as if it were the prior hub workload. The Deep workload is unchanged.
The new hub route passes an actual 30-second rendered check: 1,595.9 px travel
(about 53.2 px/s), 37.925 average FPS. This proves the revised route functions;
it is neither sustained-performance acceptance nor a claimed speedup.

The next useful performance investigation is the remaining rendering/light
cost plus equivalent localized Depth-2 invalidation. Continue native animation
and the rest of the full premium-polish mandate after a durable checkpoint.

## Confirmed remote storage

Mats explicitly approved this code-and-evidence upload on 16 September 2026.
The earlier automatic approval rejection is resolved for this checkpoint.
The tested payload is saved on the existing work branch:

- Repository: `Corpax88/Ever-Deeper`.
- Branch: `codex/premium-polish-recovery-20260915`.
- Confirmed source checkpoint: `8328622b7f15273fef6458aacaec8f50cd7558c3`.
- Verified complete tree: `51bc26bd43f6298ed08aa7827213c5a2081de49c`.
- This tree is identical to local reviewed commit `fc7b561da452b067e1bab191c7b446308487f299`.
- Follow-up commits record storage only; the tests above apply to this exact source.

The branch ref was read back after the fast-forward update. The original
previous-session working tree remains intact. The current local continuation is
`/workspace/scratch/5a78be25fc28/Ever-Deeper`; scratch paths are disposable,
and the verified remote branch is authoritative. Re-read its current head before
editing. No game version was deployed; sustained FPS and full polish remain open.

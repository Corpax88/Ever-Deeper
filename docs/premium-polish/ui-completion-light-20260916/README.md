# Completed workshop benefits and light preview review

The three runtime files from the reviewed UI candidate `36a286cf1867d82b1c8f35cf7bab2577bbea7e68`
were restored exactly from the surviving worktree. `ui-change-receipt.json` binds
all three SHA-256 hashes to the actual native captures. Its pending notes describe
that candidate checkpoint, not a completed combined browser run.

Completed Treasure Chamber and Tunnel Workshop cards show their active benefits
without implying another upgrade. Ordinary Forge keeps its real before/after
values. The Light Lab uses an equally framed 1100×900 cave preview for every
style, readable thumbnails and accurate beam-width labels. Light range, energy,
production beam identities and original PNGs are unchanged.

An independent critic reviewed four actual 1696×780 station captures and eight
light states. Those original three findings pass this bounded native review.
Maximum Wide remains inside the preview with visible margins. Current/restored
frames match exactly. The critic noted minor selected-button emphasis polish;
there is no whole-game, browser or physical-device approval from this review.

The final source touch check passes 123; premium core's 200 passed before the
last thumbnail/label refinement. A combined immutable-PCK review must cover the
integrated result. The residency harness now explicitly expects the reviewed
1100×900 beam size instead of 1100×1200; close/reopen resource checks remain
unchanged and require a fresh rendered exact-package run.

Preserve the preceding invalid PNG movie attempt (`ui-completion-stations-native`):
it forced 1280×720 and cropped the 1696×780 interface. Only the later direct
viewport captures are accepted. Raw evidence is outside the source repository.

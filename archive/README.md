# Historical sources

These notes document earlier releases. The retired HTML runtime and its tests were
removed during the premium polish; recover them from git history if needed.
The remaining notes are excluded from Godot exports and are not current QA.
Use git history at commit 090a020 for an intact historical distribution snapshot.
The root project.godot and README describe the current complete source.

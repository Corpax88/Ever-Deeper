# Rejected return studies16H–16J

These are archived diagnostic producers, not an animation bank or production code.
Do not import these candidates into the active runtime. The active study remains16G,
which also remains rejected. No9/10 result exists.

16H clears the whole recorded return/lift with a rigid translation, but fails full
arm rotations.16I adds all four original-frame transports and one constrained
path solve. Its1450-pose analytical gate passes, but five actual Blender images
hide the pick at phases0 and.131: the independent visual critic rejects it.
16J removes the extra tool/torso turn and introduces body-silhouette support-plane
constraints. Its one solve fails. A separate linear feasibility check confirms
that the selected fixed support-plane/spline corridor is empty. This does not
prove the motion task or every possible path is physically impossible.

The sources retain original evidence paths for exact recovery. source-map.json
maps all Python evidence files to their authoritative Git path and SHA. Numeric
exports, original images, reports and manifests are in the corresponding saved
Ever-Deeper-retur-16H-16J-20260919.zip evidence bundle. Python source is not duplicated
in that bundle; reconstruct frozen directories using this mapping if necessary.
The16H/16I producers reuse the frozen16G/D/E inputs identified by their checksums
and the approved original v28/Worn files. No private Blender originals are here.

Next: derive a feasible visible pose corridor before another nonlinear solve.
The16J fixed separating planes are conservative modeling choices; inspect their
conflict before inferring physical impossibility. Retain the ore's complete
pulse envelope and independent visual/arm gates. Do not restart the failed
coefficient sets, lower the old arm/ore thresholds, or start a full bank/film.
See docs/premium-polish/hero-return-16h-16j-20260919.md for decisions and evidence.

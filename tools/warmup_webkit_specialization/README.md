# Complete candidate: pre-navigation shader API history

**HOLD — the bound full candidate failed WebGL regression gates.**
Run35248670186 completed with seven failed touch suites, across Chromium and
Mac WebKit. The actual pause assertions pass, but both engines report
`INVALID_OPERATION: bindBuffer: element array buffers can not be bound to a different target`
and `bufferSubData: no buffer`. Preserve this preparation without a REQUEST or
browser session. Resolve the candidate defect, review the new exact source/export
and intentionally rebind this study before requesting a session. None has run.

This is a diagnostic, not a release or a performance comparison. Use the actual
full export of `5e48e4e1c256bd60aef1c4ba94110ccb1ac802b9` from run35248670186,
candidate artifact10509270459, with all nine exact file identities in package-pins.json.
The candidate contains the original game plus the isolated first-frame helper;
there is no replacement Main, package overlay, seed override or resource injection.
The compiled candidate still labels itself DEV13 because it is an unpublished study.

The preceding native mechanism result independently passed in e72655d: an actual
light changed144opaque target pixels RGB51→76, original material identities matched,
and transient objects/callback disappeared. That fixture used original compiled
DEV13 Main and did not exercise the new Main source hook. Full import/export and
15core/both-flavor tests of the real candidate passed; the complete review is red
as detailed above. Broader startup/input/pixel/performance adoption gates remain open.

## One bounded session

Run one fresh ordinary-user Mac WebKit session with the original untouched observer,
resource recorder, identity decoder and helper files from1a85baf. The actual new
index.js is byte-identical to the inspected engine dispatch, independently checked.
All original method forwarding, descriptor restoration, error/drop/capacity limits
and normal UI navigation/mining/save obligations remain. Retained callback timing
is overhead-bearing engine-loop cadence, never presented FPS or a speedup claim.

After the ordinary rendered menu/version/OCR gate, before the first scripted NEW GAME
tap, collect one atomic scalar recorder/observer/touch/key snapshot. This follows
the existing initial focus operations. Call its identity event count B. Preserve
the later measurement-start receipt and the unchanged60-second measurement block.
The new receipt is the sole insertion in the old navigation span; prepare.py proves
that removing that exact insertion recovers the old route hash.

After recording, replay all original identity events, including untimed navigation.
Require exactly one link with both exact full shader text hashes and the original
UTF-16 pair fingerprint. Its compile/attach/link history and a linked-program status
query must exist by B. Reject later matching-stage compile calls even if they never
link; reject another exact-pair link on either a new program or the existing one.
Use then-assigned source versions for compile events and original compiled-version
snapshots for links. Verify prefix identity counts and one-based event boundaries.
Reject context loss/pagehide/callback replacement anywhere before recording ends,
unexpected main-page navigation, and visibility/surface changes after the menu boundary.
The existing restore gates remain mandatory.27targeted classifier checks cover
off-by-one boundaries, duplicates/relinks, untimed and timed standalone recompiles,
uncompiled source reassignment, missing/full-text mismatch, drops and continuity loss.

## Limits

`resources.stop()` freezes identity immediately after the measured60-second endpoint.
The later draw census, key release, save wait, screenshot and browser cleanup retain
their existing functional checks but have no subsequent shader-event coverage.
Recorder `success=1` means the original API call returned without throwing; status
return booleans are not captured. There is no useProgram/deleteProgram or draw-to-
program observation. Passing proves bounded original API history, not successful
GPU status measurements, gameplay cache use or absence of driver-internal work.
The boundary is a normal menu boundary, not proof of the first frame, every input,
or direct helper attribution. No original no-helper control is run in this session.
No startup parity, physical iPhone, performance or publication approval follows.

## Preparation and request

No old REQUEST is copied. First independently review this complete preparation,
exact package bindings, unchanged spans and classifier checks. Save its Git commit.
Then add only a fresh REQUEST.json as its direct child, bound to this source and
identity, one session, probe `pre_navigation_full_shader_history_v1`, attempt1.
The workflow triggers only that request path on this separate study branch.
Any failed or incompatible result is retained; no automatic rerun or silent target
hash change. The currently published DEV13 package remains the earlier5ca6f0f export.

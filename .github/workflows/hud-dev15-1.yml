name: Verify HUD and tooltips DEV15.1
on:
  push:
    branches: [codex/gameplay-icons-tooltips-20260923]
    paths: ['.github/hud-dev15-1/build.py', '.github/hud-dev15-1/native-input.json', '.github/hud-dev15-1/shell-input.json', 'scripts/**', 'assets/ui/skills/**', 'assets/ui/fonts/EBGaramond.ttf', '.github/hud-dev15-1/**', '.github/workflows/hud-dev15-1.yml']
permissions:
  contents: read
concurrency:
  group: hud-dev15-1-qa
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Install matching engine
        run: |
          curl -L --fail --retry 2 https://github.com/godotengine/godot-builds/releases/download/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip -o "$RUNNER_TEMP/godot.zip"
          unzip -q "$RUNNER_TEMP/godot.zip" -d "$RUNNER_TEMP/godot"
          echo "$RUNNER_TEMP/godot" >> "$GITHUB_PATH"
      - name: Export exact candidate
        run: python3 .github/hud-dev15-1/build.py Godot_v4.7.2-stable_linux.x86_64 "$RUNNER_TEMP/dev15-1"
      - name: Record protected-file differences
        continue-on-error: true
        run: python3 tools/check_invariants.py > "$RUNNER_TEMP/dev15-1/invariants.log" 2>&1
      - name: Check actual exported gameplay, input, touch and save behavior
        run: |
          python3 tools/qa.py --godot Godot_v4.7.2-stable_linux.x86_64 --pack "$RUNNER_TEMP/dev15-1/candidate/index.pck" --cases input overhaul touch layout build-flavor --output "$RUNNER_TEMP/dev15-1/core"
          mkdir -p "$RUNNER_TEMP/skills-isolated"
          cd "$RUNNER_TEMP/skills-isolated"
          Godot_v4.7.2-stable_linux.x86_64 --headless --path . --main-pack "$RUNNER_TEMP/dev15-1/candidate/index.pck" -- --qa-miner-skills > "$RUNNER_TEMP/dev15-1/skills-core.log" 2>&1
          python3 -c 'import os,pathlib; s=(pathlib.Path(os.environ["RUNNER_TEMP"])/"dev15-1/skills-core.log").read_text(); assert "MINER_SKILLS_COMPLETE" in s and "ERROR" not in s'
          python3 "$GITHUB_WORKSPACE/.github/hud-dev15-1/finalize-build.py" "$RUNNER_TEMP/dev15-1"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dev15-1-candidate
          path: ${{ runner.temp }}/dev15-1/candidate
          if-no-files-found: error
          retention-days: 90
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dev15-1-build-review
          path: |
            ${{ runner.temp }}/dev15-1/*.json
            ${{ runner.temp }}/dev15-1/*.log
            ${{ runner.temp }}/dev15-1/core
          if-no-files-found: error
          retention-days: 90
  browser:
    needs: build
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/download-artifact@v4
        with:
          name: dev15-1-candidate
          path: ${{ runner.temp }}/candidate
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm ci --ignore-scripts && npx playwright install chromium webkit
      - name: Actual touch and graphical Skills review
        run: node .github/hud-dev15-1/review.mjs "$RUNNER_TEMP/candidate" "$RUNNER_TEMP/dev15-1-browser"
      - name: Ordinary Apple WebKit new and saved games
        env:
          EXPECTED_VERSION: 1.0.0-dev.15.1
          CANDIDATE_SOURCE: ${{ github.sha }}
        run: node .github/dev14-startup/inspect.mjs "$RUNNER_TEMP/candidate" "$RUNNER_TEMP/dev15-1-webkit"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: dev15-1-browser-review
          path: |
            ${{ runner.temp }}/dev15-1-browser
            ${{ runner.temp }}/dev15-1-webkit
          if-no-files-found: error
          retention-days: 90

name: Trace WebGL probe warning on identical candidate
on:
  push:
    branches: [codex-render-probe]
    paths: ['.github/workflows/render-probe-web-trace.yml', '.github/dev-render-probe/web-trace.mjs']
permissions:
  contents: read
  actions: read
jobs:
  trace:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    strategy:
      fail-fast: false
      matrix:
        skip_context: ['0', '1']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: dev-render-probe-candidate
          run-id: 34223377725
          github-token: ${{ github.token }}
          path: ${{ runner.temp }}/candidate
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Compare existing context observer with an omitted observer
        env:
          PROBE_GL_TRACE: '0'
          PROBE_SKIP_CONTEXT: ${{ matrix.skip_context }}
        run: |
          npm ci
          npx playwright install --with-deps webkit
          node .github/dev-render-probe/web-trace.mjs "$RUNNER_TEMP/candidate" "$RUNNER_TEMP/review" hub
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: render-probe-context-control-${{ matrix.skip_context }}
          path: ${{ runner.temp }}/review
          retention-days: 30

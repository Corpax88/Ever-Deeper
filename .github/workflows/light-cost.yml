name: Isolate lighting cost
on:
  push:
    branches: [codex-light-cost]
    paths: ['scripts/qa/suites/light_cost.gd', 'scripts/qa/suites/mobile_performance.gd', '.github/workflows/light-cost.yml']
permissions:
  contents: read
jobs:
  rendered:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        area: [hub, mossvein]
        resolution: [844x390, 2328x1260]
    steps:
      - uses: actions/checkout@v4
      - name: Install matching native renderer
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y xvfb libgl1-mesa-dri ripgrep
          curl -L --fail --retry 3 --retry-all-errors https://github.com/godotengine/godot-builds/releases/download/4.7.2-stable/Godot_v4.7.2-stable_linux.x86_64.zip -o "$RUNNER_TEMP/godot.zip"
          unzip -q "$RUNNER_TEMP/godot.zip" -d "$RUNNER_TEMP/godot"
          echo "$RUNNER_TEMP/godot" >> "$GITHUB_PATH"
      - name: Import exact study source
        run: |
          mkdir -p "$RUNNER_TEMP/review"
          Godot_v4.7.2-stable_linux.x86_64 --headless --editor --path . --import > "$RUNNER_TEMP/review/import.log" 2>&1
          if rg 'SCRIPT ERROR|Parse Error|^ERROR:' "$RUNNER_TEMP/review/import.log"; then exit 1; fi
          python3 tools/check_invariants.py
      - name: Warm baseline, grouped lights, individual lights and restoration
        env:
          LIBGL_ALWAYS_SOFTWARE: '1'
        run: |
          xvfb-run -a -s '-screen 0 2560x1440x24' timeout 420 Godot_v4.7.2-stable_linux.x86_64 --path . --resolution '${{ matrix.resolution }}' --audio-driver Dummy -- --qa-mobile-performance --perf-light-cost --probe-area='${{ matrix.area }}' --perf-output="$RUNNER_TEMP/review" > "$RUNNER_TEMP/review/native.log" 2>&1
          if rg 'SCRIPT ERROR|Parse Error|^ERROR:' "$RUNNER_TEMP/review/native.log"; then exit 1; fi
          rg LIGHT_COST_COMPLETE "$RUNNER_TEMP/review/native.log"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: light-cost-${{ matrix.area }}-${{ matrix.resolution }}
          path: ${{ runner.temp }}/review
          retention-days: 30
